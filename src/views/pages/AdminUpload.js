import React, {Component} from 'react';

import Header from './../widgets/Header';
import Footer from './../widgets/Footer';


//todo when this page loads then check for the private key stored in cookies data
// and check its validity on server, if not correct then redirect to adminLogin page
class AdminUpload extends Component {
    render() {
        return (
            <div>
                AdminUpload
            </div>
        );
    }
}

export default AdminUpload;
